#include<bits/stdc++.h>
using namespace std;
int main(){
    stack<int>st;
    //st.push(5);
    //st.push(3);
    //st.push_back(4);
    //while(!st.empty())
   // { 
     cout<<st.top();// always check stack must not be empty before using top();
     //st.pop();
   // }
}